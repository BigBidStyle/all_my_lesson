Области видимости.

Локальная область видимости (local scope) <--  Является самой внутренней областью, которая содержит список локальных
имен, доступных в текущей функции.

Область всех закрывающихся функций (enclosing scope) <-- Поиск имени начинается с ближайшей охватывающей области и
перемещается наружу.

Глобальная область (global scope) <-- Область уровня модуля, содержащая все глобальные имена из текущего модуля.
Следующий уровень поиска имени.

Встроена область (build-in scope) <-- Содержит список всех встроенных имен. Поиск имени в этой области выполняется
последним.
# -------------------------------------------------------------------------------------------------------------------- #

